[package]
name = "pnpd"
version = "0.5.0"
edition = "2021"
description = "Peios Network Policy daemon: observation, the verdict stream, and policy authoring"
license = "MIT"

[dependencies]
libc = { workspace = true }
peios = { workspace = true }
