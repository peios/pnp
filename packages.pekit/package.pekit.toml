format = "peipkg"

[package]
# NOTE: 0.2.0-1 was republished in place with the JSON fix (dev keyring,
# same revision, new bits — hygiene sin confessed). 0.3.0 speaks kernel
# ABI 2 (machinery slice); revisions restart at -1. 0.5.0 speaks kernel
# ABI 4 (the identity facts); revisions restart at -1.
version      = "{{version}}-1"
architecture = "x86_64"
license      = "MIT"
