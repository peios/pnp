format = "peipkg"

[package]
# NOTE: 0.2.0-1 was republished in place with the JSON fix (dev keyring,
# same revision, new bits — hygiene sin confessed); next publish is -2.
version      = "{{version}}-3"
architecture = "x86_64"
license      = "MIT"
